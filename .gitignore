# ── 파이썬 환경 / 캐시 ──
.venv/
__pycache__/
*.pyc
.pytest_cache/

# ── 비밀키 (절대 커밋 금지) ──
.env

# ── 서류행정 산출물 (개인정보 포함. 절대 커밋 금지) ──
output/
audit/
mcp_servers/docs/output/

# ── OS / 에디터 / AI 도구 임시 ──
.DS_Store
.claude/
.understand-anything/
.superpowers/

# ── 대용량 미디어 ──
# 시연영상은 데이콘에 직접 업로드. 깃에는 제외하되 제출 압축본엔 수동 포함.
제출결과물/*.mp4
*.mov
*.mp3
*.srt

# ── 시연 실행 스크립트 (팀 로컬용. 깃 제외) ──
시연_서버_켜기.command
시연_시작_맥.command
시연_시작_윈도우.bat
